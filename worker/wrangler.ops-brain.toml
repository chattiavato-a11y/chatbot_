# worker/wrangler.ops-brain.toml

name = "ops-brain"
main = "ops-brain.js"
compatibility_date = "2026-01-05"

# Brain can stay on workers.dev for debugging.
# It still returns 404 publicly because ops-brain.js only serves host "brain.local".
workers_dev = true

[ai]
binding = "AI"

